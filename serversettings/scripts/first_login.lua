-- first_login is called when a player logs in and doesn't have any missions.
-- use it to load whatever initial mission makes sense for this player's class.
function main()
	mission.load("welcome")
end
